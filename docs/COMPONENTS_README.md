# UI Components - Food Tracker

Библиотека переиспользуемых UI компонентов для Food Tracker, реализованных согласно дизайн-спецификации.

## Список компонентов

1. [CircularProgress](#1-circularprogress) - Кольцевая диаграмма прогресса
2. [MacroProgressBar](#2-macroprogressbar) - Прогресс-бары макронутриентов
3. [MealCard](#3-mealcard) - Карточка приёма пищи
4. [ProductCard](#4-productcard) - Карточка продукта в поиске
5. [NutrientGrid](#5-nutrientgrid) - Сетка макронутриентов
6. [BarChart](#6-barchart) - Столбчатая диаграмма
7. [FoodTrackerToggle](#7-foodtrackertoggle) - Переключатель

---

## 1. CircularProgress

**Файл:** `CircularProgress.kt`

**Описание:** Кольцевая диаграмма прогресса (CircularProgressIndicator) для отображения калорий, макронутриентов и других показателей.

### Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `value` | Float | Текущее значение |
| `max` | Float | Максимальное значение (цель) |
| `size` | Dp | Размер диаграммы (по умолчанию 140dp) |
| `strokeWidth` | Dp | Толщина кольца (по умолчанию 12dp) |
| `color` | Color | Цвет прогресса (по умолчанию Primary) |
| `backgroundColor` | Color | Цвет фона трека (по умолчанию color с alpha 0.2) |
| `content` | @Composable BoxScope.() -> Unit | Контент внутри кольца |

### Использование

```kotlin
CircularProgress(
    value = 684f,
    max = 2200f,
    size = 130.dp,
    strokeWidth = 10.dp,
    color = Color.White
) {
    Column(horizontalAlignment = Alignment.CenterHorizontally) {
        Text(text = "684", style = MaterialTheme.typography.displayMedium)
        Text(text = "из 2200 ккал", style = MaterialTheme.typography.labelSmall)
    }
}
```

### Где используется

- **HomeScreen:** Кольцевая диаграмма калорий в header
- **ProductDetailScreen:** Диаграмма калорий продукта

---

## 2. MacroProgressBar

**Файл:** `MacroProgressBar.kt`

**Описание:** Горизонтальный прогресс-бар для макронутриентов (Б/Ж/У/Клетчатка).

### Варианты

#### MacroProgressBar (обычный)
Для использования на обычном фоне.

#### MacroProgressBarWhite
Для использования на gradient header с белым текстом.

### Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `label` | String | Название нутриента (например, "Белки") |
| `value` | Float | Текущее значение |
| `max` | Float | Максимальное значение (цель) |
| `unit` | String | Единица измерения (например, "г") |
| `color` | Color | Цвет прогресс-бара |

### Использование

```kotlin
// Обычный вариант
MacroProgressBar(
    label = "Белки",
    value = 56f,
    max = 140f,
    unit = "г",
    color = ProteinColor
)

// Вариант для белого фона
MacroProgressBarWhite(
    label = "Белки",
    value = 56f,
    max = 140f,
    unit = "г",
    color = ProteinColor
)
```

### Где используется

- **HomeScreen:** Прогресс-бары Б/Ж/У/Клетчатки в header

---

## 3. MealCard

**Файл:** `MealCard.kt`

**Описание:** Карточка приёма пищи с emoji, названием, временем, итоговыми калориями и списком продуктов.

### Модели данных

```kotlin
data class FoodItemData(
    val name: String,
    val weight: String,
    val calories: Int
)
```

### Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `emoji` | String | Иконка приёма (🌅, ☀️, 🌙, 🍎) |
| `name` | String | Название приёма ("Завтрак", "Обед"...) |
| `time` | String? | Время приёма ("08:00") или null |
| `totalCalories` | Int | Итоговые калории приёма |
| `foodItems` | List\<FoodItemData\> | Список продуктов |
| `onAddClick` | () -> Unit | Обработчик кнопки "+" |

### Использование

```kotlin
MealCard(
    emoji = "🌅",
    name = "Завтрак",
    time = "08:00",
    totalCalories = 303,
    foodItems = listOf(
        FoodItemData("Овсяная каша", "200г", 150),
        FoodItemData("Банан", "1 шт", 89),
        FoodItemData("Мёд", "1 ст.л.", 64)
    ),
    onAddClick = { /* navigate to search */ }
)
```

### Где используется

- **HomeScreen:** Список приёмов пищи за день

---

## 4. ProductCard

**Файл:** `ProductCard.kt`

**Описание:** Карточка продукта в списке поиска с макронутриентами, калориями и кнопкой избранного.

### Модели данных

```kotlin
data class ProductData(
    val id: String,
    val name: String,
    val brand: String?,
    val portion: String,
    val calories: Int,
    val protein: Float,
    val fat: Float,
    val carbs: Float,
    val isFavorite: Boolean = false
)
```

### Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `product` | ProductData | Данные продукта |
| `onClick` | () -> Unit | Обработчик клика по карточке |
| `onFavoriteClick` | () -> Unit | Обработчик кнопки избранного |

### Использование

```kotlin
ProductCard(
    product = ProductData(
        id = "1",
        name = "Куриная грудка",
        brand = "без бренда",
        portion = "100г",
        calories = 110,
        protein = 23.1f,
        fat = 1.2f,
        carbs = 0f,
        isFavorite = false
    ),
    onClick = { /* navigate to product detail */ },
    onFavoriteClick = { /* toggle favorite */ }
)
```

### Где используется

- **SearchScreen:** Список результатов поиска

---

## 5. NutrientGrid

**Файл:** `NutrientGrid.kt`

**Описание:** Сетка макронутриентов (Grid 2 колонки) для экрана продукта. Калории занимают всю ширину.

### Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `calories` | String | Калории |
| `protein` | String | Белки |
| `fat` | String | Жиры |
| `carbs` | String | Углеводы |
| `fiber` | String? | Клетчатка (опционально) |

### Использование

```kotlin
NutrientGrid(
    calories = "165",
    protein = "34.7",
    fat = "1.8",
    carbs = "0.0",
    fiber = "0.0"
)
```

### Где используется

- **ProductDetailScreen:** Отображение макронутриентов продукта

---

## 6. BarChart

**Файл:** `BarChart.kt`

**Описание:** Столбчатая диаграмма для отображения статистики калорий и динамики веса.

### Модели данных

```kotlin
data class BarData(
    val label: String,
    val value: Float,
    val color: Color? = null
)
```

### Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `data` | List\<BarData\> | Данные для столбцов |
| `height` | Dp | Высота диаграммы (по умолчанию 130dp) |
| `showValues` | Boolean | Показывать значения над столбцами (по умолчанию true) |
| `showLabels` | Boolean | Показывать подписи под столбцами (по умолчанию true) |
| `maxValue` | Float? | Максимальное значение шкалы (null = автоматически) |
| `defaultColor` | Color | Цвет по умолчанию (по умолчанию Primary) |
| `gradientColors` | List\<Color\>? | Градиент для столбцов (null = сплошной цвет) |

### Использование

```kotlin
// Статистика калорий
BarChart(
    data = listOf(
        BarData("Пн", 1850f),
        BarData("Вт", 2100f),
        BarData("Ср", 1950f),
        BarData("Чт", 2250f, color = Danger), // Превышение
        BarData("Пт", 1780f),
        BarData("Сб", 2050f),
        BarData("Вс", 1680f)
    ),
    height = 130.dp,
    defaultColor = Color.White,
    gradientColors = listOf(
        Color.White.copy(alpha = 0.9f),
        Color.White.copy(alpha = 0.5f)
    )
)

// Динамика веса
BarChart(
    data = listOf(
        BarData("Янв", 135f),
        BarData("Фев", 133.5f),
        BarData("Мар", 132f),
        // ...
    ),
    height = 120.dp,
    defaultColor = Primary
)
```

### Где используется

- **StatsScreen:** Столбчатая диаграмма калорий за неделю/месяц
- **ProfileScreen:** Динамика веса по месяцам

---

## 7. FoodTrackerToggle

**Файл:** `Toggle.kt`

**Описание:** Кастомный переключатель (Toggle/Switch) для настроек напоминаний.

### Параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `checked` | Boolean | Состояние (включён/выключен) |
| `onCheckedChange` | (Boolean) -> Unit | Обработчик изменения |
| `enabled` | Boolean | Активность (по умолчанию true) |

### Использование

```kotlin
var isEnabled by remember { mutableStateOf(true) }

FoodTrackerToggle(
    checked = isEnabled,
    onCheckedChange = { isEnabled = it }
)
```

### Где используется

- **ProfileScreen:** Настройки напоминаний (Завтрак, Обед, Ужин, Умные напоминания)

---

## Дизайн-система

### Цвета нутриентов

| Нутриент | Цвет | HEX |
|----------|------|-----|
| Белки (Protein) | `ProteinColor` | `#5B8DEF` |
| Жиры (Fat) | `FatColor` | `#E63946` |
| Углеводы (Carbs) | `CarbsColor` | `#FFBF69` |
| Клетчатка (Fiber) | `FiberColor` | `#2EC4B6` |

### Типографика

Все компоненты используют MaterialTheme.typography:
- `displayLarge` / `displayMedium` - большие цифры (калории)
- `titleLarge` / `titleMedium` / `titleSmall` - заголовки
- `bodyLarge` / `bodyMedium` - обычный текст
- `labelLarge` / `labelMedium` / `labelSmall` - подписи

### Скругления

- Карточки: `RoundedCornerShape(14.dp)` или `16.dp`
- Кнопки: `RoundedCornerShape(10.dp)`
- Прогресс-бары: `RoundedCornerShape(3.dp)` или `4.dp`
- Столбцы диаграмм: `CornerRadius(8.dp)`

---

## Preview

Все компоненты имеют `@Preview` функции для быстрого просмотра в Android Studio:

1. Откройте любой файл компонента (например, `CircularProgress.kt`)
2. Найдите функции с аннотацией `@Preview`
3. Нажмите иконку 👁️ рядом с функцией
4. Увидите визуальное представление компонента

Каждый компонент имеет несколько preview:
- Обычное состояние
- Пустое состояние (если применимо)
- Заполненное состояние (если применимо)
- Тёмная тема
- Интерактивное (если применимо)

---

## Тестирование

Все компоненты:
- ✅ Скомпилированы без ошибок
- ✅ Поддерживают светлую и тёмную темы
- ✅ Имеют preview для визуальной проверки
- ✅ Следуют дизайн-спецификации
- ✅ Анимированы (где применимо)
- ✅ Адаптивны (fillMaxWidth, weight)

---

## Следующие шаги

После реализации экранов:
1. Интеграция компонентов в HomeScreen
2. Интеграция компонентов в SearchScreen
3. Интеграция компонентов в ProductDetailScreen
4. Интеграция компонентов в StatsScreen
5. Интеграция компонентов в ProfileScreen

---

## Примечания

- Все компоненты принимают `Modifier` параметр для гибкости
- Все компоненты используют MaterialTheme для цветов и типографики
- Все анимации используют `animateFloatAsState` / `animateDpAsState`
- Все карточки имеют тень и границу согласно спецификации
