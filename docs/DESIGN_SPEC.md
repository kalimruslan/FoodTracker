# Food Tracker — Дизайн-спецификация

## Общая информация

**Название:** Food Tracker
**Платформа:** Android (Jetpack Compose, Material Design 3)
**Тема:** Светлая + Тёмная + Системная
**Стиль:** Яркий, мотивирующий (вдохновлён MyFitnessPal)
**Шрифт:** Nunito (или системный sans-serif как fallback)
**Навигация:** Bottom Navigation Bar (4 таба)
**Язык интерфейса:** Русский
**Референс прототипа:** см. файл `food-tracker.jsx` (React-прототип)

---

## Цветовая палитра

### Светлая тема

| Назначение | Цвет | HEX |
|---|---|---|
| Primary | Оранжевый | `#FF6B35` |
| Primary Light | Светло-оранжевый | `#FF8F5E` |
| Primary Dark | Тёмно-оранжевый | `#E55A25` |
| Secondary | Бирюзовый | `#2EC4B6` |
| Secondary Light | Светло-бирюзовый | `#4DD4C8` |
| Accent | Золотистый | `#FFBF69` |
| Danger / Fat color | Красный | `#E63946` |
| Success | Бирюзовый | `#2EC4B6` |
| Warning | Золотистый | `#FFBF69` |
| Background | Светло-серый | `#FAFAFA` |
| Card Background | Белый | `#FFFFFF` |
| Text Primary | Тёмно-синий | `#1A1A2E` |
| Text Secondary | Серый | `#6B7280` |
| Border | Светло-серый | `#E5E7EB` |

### Цвета нутриентов (одинаковы для обеих тем)

| Нутриент | Цвет | HEX |
|---|---|---|
| Белки (Protein) | Синий | `#5B8DEF` |
| Углеводы (Carbs) | Золотистый | `#FFBF69` |
| Жиры (Fat) | Красный | `#E63946` |
| Клетчатка (Fiber) | Бирюзовый | `#2EC4B6` |

### Тёмная тема

| Назначение | HEX |
|---|---|
| Background | `#1A1A2E` |
| Card Background | `#252542` |
| Text Primary | `#FFFFFF` |
| Text Secondary | `#9CA3AF` |
| Border | `#374151` |

Primary, Secondary, Accent, нутриенты — остаются те же.

---

## Типографика

| Элемент | Размер | Вес | Примечание |
|---|---|---|---|
| Заголовок экрана | 18-20sp | ExtraBold (800) | Nunito |
| Заголовок секции | 16-17sp | ExtraBold (800) | Nunito |
| Название продукта/приёма | 14-15sp | Bold (700) | Nunito |
| Обычный текст | 13-14sp | Regular (400) | |
| Подпись/мета | 10-12sp | SemiBold (600) | Text Secondary |
| Большие цифры (калории) | 28-36sp | ExtraBold (800) | Nunito |

---

## Скругления и отступы

| Элемент | Значение |
|---|---|
| Карточки | 14-16dp corner radius |
| Кнопки | 10-16dp corner radius |
| Прогресс-бары | 3-4dp corner radius |
| Header gradient | 0 0 28dp 28dp (только нижние углы) |
| Padding экрана | 16dp горизонтально |
| Расстояние между карточками | 8-10dp |
| Padding внутри карточки | 12-16dp |

---

## Навигация (Bottom Navigation Bar)

4 таба:

| Таб | Иконка | Label |
|---|---|---|
| Главная | Home (🏠) | "Главная" |
| Поиск | Search (🔍) | "Поиск" |
| Статистика | BarChart (📊) | "Статистика" |
| Профиль | Person (👤) | "Профиль" |

- Активный таб: цвет Primary (`#FF6B35`), жирный текст, точка-индикатор снизу
- Неактивный: серый (`#6B7280`), пониженная непрозрачность
- Фон: белый с верхним border

---

## Экран 1: Главная (Дневник)

### Структура сверху вниз:

#### 1.1 Header (gradient background: Primary → Primary Dark, скругление снизу 28dp)

**Верхняя строка:**
- Слева: "Сегодня" (13sp, opacity 0.85) + дата "12 февраля" (20sp, ExtraBold, белый)
- Справа: иконка календаря 📅 + иконка уведомлений 🔔 (в кружках 36x36 с rgba(255,255,255,0.2) фоном, скругление 12dp)

**Выбор дня недели:**
- Горизонтальный ряд из 7 дней (Пн-Вс)
- Каждый день: сокращённое название (10sp, opacity 0.7) + число (15sp)
- Выбранный день: фон rgba(255,255,255,0.25), скругление 12dp, число Bold
- Ширина каждого элемента: 40dp

**Кольцевая диаграмма + макронутриенты (горизонтально):**
- Слева: CircularProgressIndicator
  - Размер: 130dp
  - Толщина: 10dp
  - Цвет: белый (трек: rgba(255,255,255,0.2))
  - Внутри: потреблённые ккал (28sp, ExtraBold) + "из {target} ккал" (10sp) + "Осталось {remaining}" (11sp)
- Справа: 4 горизонтальных прогресс-бара (на белом фоне header'a)
  - Белки: цвет `#5B8DEF`, label + значение "{value}/{max}г"
  - Жиры: цвет `#E63946`
  - Углеводы: цвет `#FFBF69`
  - Клетчатка: цвет `#2EC4B6`
  - Высота бара: 6dp, скругление 3dp
  - Трек: rgba(255,255,255,0.2)
  - Текст: белый, 11sp

#### 1.2 Приёмы пищи (на основном фоне)

**Заголовок секции:**
- "Приёмы пищи" (17sp, ExtraBold) + справа "+ Добавить приём" (12sp, Primary, SemiBold)

**Карточки приёмов пищи (MealCard):**
- Фон: белый, скругление 16dp, тень 0 2 8 rgba(0,0,0,0.04), border 1dp
- Строка: emoji иконка (24sp) + название приёма (15sp, Bold) + время (12sp, серый) | справа: итого ккал (14sp, Bold, Primary) + кнопка "+" (32x32, gradient Primary, белый текст, скругление 10dp, тень)
- Если есть продукты — разделительная линия, список:
  - Название продукта (13sp) + вес (11sp, серый) | справа: ккал (13sp, серый, SemiBold)

**Стандартные приёмы:**
1. 🌅 Завтрак (08:00)
2. ☀️ Обед (13:00)
3. 🌙 Ужин (19:00)
4. 🍎 Перекус (без времени)

Пользователь может добавлять кастомные приёмы.

#### 1.3 Трекер воды (опционально, внизу)

- Карточка с gradient фоном Secondary (opacity 0.15)
- 💧 "Вода" + "4 из 8 стаканов"
- Кнопка "+ Стакан" (Secondary фон, белый текст)

---

## Экран 2: Поиск продуктов

### Структура:

#### 2.1 Header
- Кнопка назад "←" (22sp) + "Добавить продукт" (18sp, ExtraBold)

#### 2.2 Строка поиска
- Фон: `#F3F4F6`, скругление 14dp, padding 10-14dp
- Иконка 🔍 слева (opacity 0.5) + input field + кнопка сканера 📷 справа (36x36, gradient Primary, скругление 10dp)
- Placeholder: "Найти продукт..."

#### 2.3 Табы (горизонтальный скролл)
- 🔍 Поиск | 🕐 Недавние | ⭐ Избранное | 📋 Рецепты
- Активный: фон Primary, белый текст
- Неактивный: фон `#F3F4F6`, серый текст
- Скругление 12dp, padding 8x14dp, шрифт 13sp SemiBold

#### 2.4 Список результатов (ProductCard)
- Фон: белый, скругление 14dp, border, тень
- Слева: название (14sp, Bold) + бренд/порция (11sp, серый) + макро-строка: "Б 23.1г  Ж 1.2г  У 0г" (11sp, каждый своим цветом, SemiBold)
- Справа: калории (16sp, ExtraBold, Primary) + "ккал" (10sp) + кнопка избранного ⭐/☆ (18sp)

---

## Экран 3: Карточка продукта

### Структура:

#### 3.1 Header (gradient Primary → Primary Dark, скругление снизу 28dp)
- Кнопка назад + "Продукт" (18sp, ExtraBold, белый)
- Название продукта (22sp, ExtraBold, белый)
- Бренд (13sp, opacity 0.8)
- Кольцевая диаграмма по центру: калории (36sp, ExtraBold) + "ккал" (12sp)
  - Размер: 150dp, толщина 12dp, цвет белый

#### 3.2 Выбор порции
- Карточка: input для веса (60dp ширина, border Primary 30% opacity, скругление 10dp, 16sp Bold, по центру)
- Переключатель единиц: "г" / "шт" / "порц" — кнопки (padding 8x12, скругление 10dp)
  - Активная: фон Primary, белый текст
  - Неактивная: фон `#F3F4F6`, серый текст
- Все значения нутриентов пересчитываются при изменении веса

#### 3.3 Макронутриенты (Grid 2 колонки)
- Калории — на всю ширину (gridColumn: span 2)
  - Цифра 28sp, остальные 22sp
- Каждая карточка: label (11sp, серый) + значение (22sp, ExtraBold, цвет нутриента) + единица (12sp, серый)
- Карточки: белый фон, скругление 14dp, border, padding 14dp

#### 3.4 Микронутриенты
- Заголовок "Микронутриенты" (16sp, ExtraBold)
- Список в одной карточке:
  - Строка: название (13sp) | значение (13sp, SemiBold, серый)
  - Разделители между строками
  - Данные: Натрий, Калий, Кальций, Железо, Витамин A, Витамин C и др.

#### 3.5 Кнопка "Добавить в {приём пищи}"
- Sticky внизу экрана
- Ширина 100%, padding 16dp, скругление 16dp
- Gradient Primary → Primary Light
- Текст: белый, 16sp, Bold
- Тень: 0 4 16 Primary (50% opacity)

---

## Экран 4: Статистика

### Структура:

#### 4.1 Header (gradient Primary)
- "Статистика" (18sp, ExtraBold, белый, по центру)
- Переключатель периода: "Неделя" / "Месяц" / "3 мес"
  - Активный: rgba(255,255,255,0.25)
  - Неактивный: rgba(255,255,255,0.1)
  - Скругление 10dp

**Столбчатая диаграмма (внутри header):**
- 7 столбцов (Пн-Вс) с калориями за каждый день
- Высота пропорциональна максимуму
- В норме: белые столбцы (gradient rgba(255,255,255, 0.9→0.5))
- Превышение цели (>2200 ккал): красные столбцы (`#E63946`)
- Подписи: значение сверху (9sp), день недели снизу (10sp)

#### 4.2 Средние показатели (Grid 2x2)
- 4 карточки: Калории (🔥), Белки (🥩), Жиры (🥑), Углеводы (🍞)
- Каждая: иконка (20sp) + label (11sp, серый) + значение (22sp, ExtraBold, цвет нутриента) + единица (10sp, серый)

#### 4.3 Распределение БЖУ
- Карточка с горизонтальным stacked bar (3 сегмента: Белки/Жиры/Углеводы)
- Высота бара: 20dp, скругление 10dp
- Под баром: 3 пункта с цветной точкой + процент (13sp, Bold) + название (10sp, серый)

---

## Экран 5: Профиль

### Структура:

#### 5.1 Header (gradient Primary, скругление снизу 28dp)
- "Профиль" (18sp, ExtraBold, белый, по центру)
- Аватар: круг 80dp, фон rgba(255,255,255,0.2), иконка 👤 (36sp)
- Имя "Пользователь" (20sp, ExtraBold)
- "Цель: 2 200 ккал/день" (13sp, opacity 0.8)

#### 5.2 Моя цель
- 3 кнопки в ряд (flex: equal):
  - 📉 Похудеть | ⚖️ Поддержать | 📈 Набрать
  - Активная: border 2dp Primary, фон Primary 10%, тень
  - Неактивная: border 1dp серый, белый фон
  - Скругление 14dp, padding 14x8dp
  - Иконка (24sp) + текст (12sp, Bold)

#### 5.3 Параметры тела
- Единая карточка-список:
  - ⚖️ Вес → 130 кг
  - 📏 Рост → 185 см
  - 🎂 Возраст → 30 лет
  - 👨 Пол → Мужской
  - 🏃 Активность → Умеренная
  - ⏱️ Темп → 0.5 кг/нед
- Каждая строка: иконка (20sp) + label (14sp) | значение (14sp, Bold, Primary) + стрелка "›"
- Разделители между строками
- По нажатию — переход к редактированию

#### 5.4 Расчёт калорий (Миффлин-Сан Жеора)
- Карточка с gradient фоном Secondary (15% opacity), border Secondary 30%
- Заголовок: "Расчёт по Миффлина-Сан Жеора" (14sp, ExtraBold)
- Строки:
  - Базовый метаболизм (BMR): 2 050 ккал
  - С учётом активности (TDEE): 2 700 ккал
  - Разделитель
  - Цель (дефицит -500): **2 200 ккал** (16sp, ExtraBold, Secondary)

**Формула Миффлина-Сан Жеора:**
- Мужчины: BMR = 10 × вес(кг) + 6.25 × рост(см) − 5 × возраст − 5 + 161
- Женщины: BMR = 10 × вес(кг) + 6.25 × рост(см) − 5 × возраст − 161
- TDEE = BMR × коэффициент активности
- Коэффициенты: Минимальная (1.2), Лёгкая (1.375), Умеренная (1.55), Высокая (1.725), Экстремальная (1.9)
- Цель = TDEE ± дефицит/профицит (зависит от темпа: 0.5кг/нед ≈ 500 ккал)

#### 5.5 Динамика веса
- Карточка со столбчатой диаграммой
- Столбцы по месяцам, текущий — gradient Primary, остальные — Primary 20-40%
- Значение веса над каждым столбцом (10sp, Bold, Primary)
- Итого: "↓ -5 кг за 7 месяцев" (12sp, Success, Bold)

#### 5.6 Фото прогресса
- Горизонтальный ряд карточек (flex)
- Каждая: 130dp высота, скругление 14dp, серый фон, иконка 📷 + месяц
- Последняя: dashed border Primary, иконка "+" — добавить фото

#### 5.7 Напоминания
- Карточка-список:
  - Завтрак — 08:00 — toggle
  - Обед — 13:00 — toggle
  - Ужин — 19:00 — toggle
  - Умные напоминания — toggle
- Toggle: 44x26dp, скругление 13dp, кружок 22dp
  - Включён: фон Primary, кружок справа
  - Выключен: фон `#D1D5DB`, кружок слева

---

## API: Open Food Facts

**Base URL:** `https://world.openfoodfacts.org/api/v2/`

**Поиск продуктов:**
```
GET /search?search_terms={query}&countries_tags=en:russia&page_size=20&json=1
```

**Поиск по штрихкоду:**
```
GET /product/{barcode}.json
```

**Поля продукта:**
- `product_name` — название
- `brands` — бренд
- `nutriments.energy-kcal_100g` — калории на 100г
- `nutriments.proteins_100g` — белки
- `nutriments.carbohydrates_100g` — углеводы
- `nutriments.fat_100g` — жиры
- `nutriments.fiber_100g` — клетчатка
- `nutriments.sodium_100g`, `potassium_100g`, `calcium_100g` и т.д.

---

## Архитектура (рекомендация)

### Стек
- **UI:** Jetpack Compose + Material Design 3
- **Архитектура:** MVVM + Clean Architecture
- **DI:** Hilt
- **БД:** Room
- **Сеть:** Retrofit + OkHttp + Kotlinx Serialization
- **Навигация:** Compose Navigation
- **Async:** Coroutines + Flow
- **Графики:** Compose Canvas (кастомные) или библиотека Vico
- **Камера (штрихкод):** ML Kit Barcode Scanning
- **Уведомления:** AlarmManager + NotificationCompat (или WorkManager)
- **Фото:** Coil для загрузки, CameraX для съёмки

### Структура модулей (рекомендация)

```
app/
├── data/
│   ├── local/          # Room DB, DAO, entities
│   ├── remote/         # Retrofit API, DTOs
│   └── repository/     # Repository implementations
├── domain/
│   ├── model/          # Domain models
│   ├── repository/     # Repository interfaces
│   └── usecase/        # Use cases
├── presentation/
│   ├── home/           # HomeScreen, HomeViewModel
│   ├── search/         # SearchScreen, SearchViewModel
│   ├── product/        # ProductScreen, ProductViewModel
│   ├── stats/          # StatsScreen, StatsViewModel
│   ├── profile/        # ProfileScreen, ProfileViewModel
│   ├── components/     # Shared UI components
│   └── theme/          # Colors, Typography, Theme
├── di/                 # Hilt modules
└── util/               # Extensions, helpers
```

### Ключевые Room-сущности

```kotlin
// Приём пищи
@Entity
data class MealEntry(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val type: MealType,           // BREAKFAST, LUNCH, DINNER, SNACK, CUSTOM
    val customName: String? = null,
    val date: LocalDate,
    val time: LocalTime?
)

// Продукт в приёме пищи
@Entity
data class FoodEntry(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val mealEntryId: Long,        // FK → MealEntry
    val productId: String,         // Open Food Facts barcode/id
    val name: String,
    val brand: String?,
    val amount: Float,
    val unit: PortionUnit,         // GRAM, PIECE, PORTION
    val caloriesPer100g: Float,
    val proteinPer100g: Float,
    val carbsPer100g: Float,
    val fatPer100g: Float,
    val fiberPer100g: Float
)

// Избранный продукт
@Entity
data class FavoriteProduct(
    @PrimaryKey val productId: String,
    val name: String,
    val brand: String?,
    val caloriesPer100g: Float,
    val proteinPer100g: Float,
    val carbsPer100g: Float,
    val fatPer100g: Float,
    val fiberPer100g: Float,
    val addedAt: Instant
)

// Свой рецепт
@Entity
data class CustomRecipe(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val totalWeight: Float         // итоговый вес блюда в граммах
)

// Ингредиент рецепта
@Entity
data class RecipeIngredient(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val recipeId: Long,            // FK → CustomRecipe
    val productId: String,
    val name: String,
    val amount: Float,
    val unit: PortionUnit
)

// Профиль пользователя
@Entity
data class UserProfile(
    @PrimaryKey val id: Int = 1,   // singleton
    val weight: Float,             // кг
    val height: Float,             // см
    val age: Int,
    val gender: Gender,            // MALE, FEMALE
    val activityLevel: ActivityLevel,
    val goal: Goal,                // LOSE, MAINTAIN, GAIN
    val weightChangeRate: Float    // кг/неделю
)

// Взвешивание
@Entity
data class WeightEntry(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val weight: Float,
    val date: LocalDate,
    val photoUri: String? = null
)

// Потребление воды
@Entity
data class WaterEntry(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val glasses: Int,
    val date: LocalDate
)
```

### Enums

```kotlin
enum class MealType { BREAKFAST, LUNCH, DINNER, SNACK, CUSTOM }
enum class PortionUnit { GRAM, PIECE, PORTION }
enum class Gender { MALE, FEMALE }
enum class ActivityLevel(val coefficient: Float) {
    SEDENTARY(1.2f),
    LIGHT(1.375f),
    MODERATE(1.55f),
    ACTIVE(1.725f),
    EXTREME(1.9f)
}
enum class Goal { LOSE, MAINTAIN, GAIN }
```

---

## Напоминания

### Фиксированные
- Пользователь задаёт время для каждого приёма пищи
- AlarmManager с точным временем → PendingIntent → NotificationCompat

### Умные
- WorkManager (периодический, раз в час)
- Проверяет: если текущее время ≈ время приёма пищи ± 30 мин и нет записей за этот приём → уведомление
- Текст: "Не забудьте записать {приём пищи}! 🍽️"

---

## Дополнительные заметки

- Все числа (калории, БЖУ) пересчитываются динамически при изменении веса порции
- Кольцевая диаграмма на главной показывает прогресс: зелёный/оранжевый при норме, красный при превышении
- Графики в статистике: красным выделяются дни с превышением суточной нормы калорий
- При первом запуске — онбординг-экран для заполнения профиля (вес, рост, возраст, пол, активность, цель)
- Данные хранятся локально (Room), без авторизации
- Сканер штрихкодов открывает камеру → ML Kit распознаёт код → запрос к Open Food Facts API
